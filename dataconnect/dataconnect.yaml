specVersion: "v1beta"
serviceId: "Landmark-App"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Landmark-App-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
